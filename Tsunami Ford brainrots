-- Ana script | Escape Tsunami Brainrot
-- SOLO TP A LA BASE | ROSA PASTEL 🌸

local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "AnaScriptGUI"
gui.Parent = game.CoreGui

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 260, 0, 170)
frame.Position = UDim2.new(0.5, -130, 0.5, -85)
frame.BackgroundColor3 = Color3.fromRGB(255, 210, 235) -- ROSA 🌸
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = gui

Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 20)

-- Título
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1,0,0,45)
title.BackgroundTransparency = 1
title.Text = "Ana script"
title.Font = Enum.Font.GothamBold
title.TextSize = 26
title.TextColor3 = Color3.fromRGB(180, 60, 120)

-- Botón TP
local tpBtn = Instance.new("TextButton", frame)
tpBtn.Size = UDim2.new(0.85,0,0,50)
tpBtn.Position = UDim2.new(0.075,0,0,80)
tpBtn.BackgroundColor3 = Color3.fromRGB(255, 160, 210)
tpBtn.TextColor3 = Color3.fromRGB(120, 30, 80)
tpBtn.Font = Enum.Font.GothamBold
tpBtn.TextSize = 18
tpBtn.Text = "🏠 Teleport a la base"
tpBtn.BorderSizePixel = 0
tpBtn.Parent = frame

Instance.new("UICorner", tpBtn).CornerRadius = UDim.new(0, 16)

-- FUNCIÓN TP A LA BASE
tpBtn.MouseButton1Click:Connect(function()
	local char = player.Character
	if char and char:FindFirstChild("HumanoidRootPart") then
		-- Coordenadas genéricas de base (seguras)
		char.HumanoidRootPart.CFrame = CFrame.new(0, 50, 0)
	end
end)
